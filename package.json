{
  "name": "cin7-omni-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the Cin7 Omni API — gives Claude full access to Contacts, Products, Stock, Sales Orders, Purchase Orders, and every other Cin7 Omni v1 API resource.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cin7-omni-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" && esbuild src/index.ts --bundle --platform=node --format=cjs --target=node18 --outfile=server/index.cjs && node -e \"require('fs').chmodSync('dist/index.js', 0o755)\"",
    "build:mcpb": "npm run build && npx -y @anthropic-ai/mcpb validate manifest.json && npx -y @anthropic-ai/mcpb pack . dist/cin7-omni.mcpb",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "test": "npm run build && node test/smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cin7",
    "cin7-omni",
    "inventory",
    "claude"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
